export type formValues = {
  login: string;
  password: string;
};

export type RegisterFormValue = {
  username: string;
  firstName: string;
  lastName: string;
  password: string;
  phone: string;
  email: string;
};